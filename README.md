# WiFi Analyzer

A Flutter application that scans nearby Wi-Fi networks and displays useful information such as the network name (SSID), MAC address (BSSID), signal strength (RSSI), estimated distance, frequency band, and channel (where supported).
